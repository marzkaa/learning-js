doctype html
html(lang='en')
    head
        include ./style.pug
        title Sign in
    body
    form(action='/auth/google')
        div.container
            p Login
            input(type='text', placeholder='login', user='login')        
            p Password
            input(type='password', placeholder='password')
            button(type='submit') Sign in