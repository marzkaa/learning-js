doctype html
html(lang='en')
    head
        include ./style.pug
        title Sign in
    body
    form(action='http://localhost:9000/auth/google', method='GET')
        div.container
            p Login
            input(type='text', placeholder='login', name='login')        
            p Password
            input(type='password', placeholder='password')
            button(type='submit') Sign in